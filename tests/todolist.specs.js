describe('Todo Add Item', function() {
    var homeUrl = browser.baseUrl;
    console.log(homeUrl);
    it('displays an empty todo list on initial load', function() {
        browser.get(homeUrl);

    });

});
